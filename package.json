{
  "name": "secure-data-vault",
  "private": true,
  "packageManager": "yarn@4.9.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A --topological run build",
    "build:packages": "yarn workspaces foreach -A --topological --include '@secure-data-vault/shared-types' --include '@secure-data-vault/audit-core' --include '@secure-data-vault/crypto-core' run build",
    "test": "yarn workspaces foreach -A run test",
    "lint": "yarn workspaces foreach -A run lint",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "dev": "docker compose up -d postgres && yarn build && yarn workspace @secure-data-vault/vault-api run db:push && concurrently --names api,ui --prefix-colors blue,green \"yarn workspace @secure-data-vault/vault-api run start:dev\" \"yarn workspace @secure-data-vault/admin-console run start\"",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.32.1",
    "@typescript-eslint/parser": "^8.32.1",
    "concurrently": "^9.2.1",
    "eslint": "^9.27.0",
    "prettier": "^3.5.3",
    "typescript": "~5.9.0"
  },
  "resolutions": {
    "esbuild": "^0.28.0",
    "fast-uri": "^3.1.2"
  }
}
